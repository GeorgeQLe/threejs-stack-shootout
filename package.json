{
  "name": "web3d-bench-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "preview": "pnpm -r preview",
    "bench:aggregate": "node packages/bench-cli/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
